node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Admin-uploaded quiz images (event content, not source)
data/uploads/

# Per-install token signing secret — never commit or share it
data/.jwt-secret

# MongoDB writes made during an outage, waiting to be replayed
data/.mongo-pending.jsonl
